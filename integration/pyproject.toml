[project]
name = "eda-demo-integration"
version = "0.1.0"
description = "End-to-end integration test suite for the EDA demo stack"
requires-python = ">=3.14"
dependencies = [
    "pytest>=8.3.0",
    "httpx>=0.28.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
